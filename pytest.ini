[pytest]
# Unit tests live in tests/. scripts/ holds hardware probe scripts whose
# filenames also match test_* — they exit at import on machines without
# the instrument attached, so they must stay out of collection.
testpaths = tests
